<%= react_component("admin/Admin", {data: User.all, roles: User.all.map { |user| user.roles }.flatten, role_types: User.all.map { |user| user.roles }.flatten.collect{ |r| r.name }.uniq, authenticity_token: form_authenticity_token, jurisdiction_paths: Hash[Jurisdiction.all.map{|jur| [jur.jurisdiction_path_string, jur.id]}] }) %>