<%= render partial: "layouts/breadcrumb", locals: { jurisdiction_path: current_user.jurisdiction_path, crumbs: [ {value: "Return To Workflow", href: '/'}, {value: "Edit Monitoree", href: nil} ] } %>

<%= react_component("enrollment/Enrollment", { current_user: current_user, patient: @patient, editMode: true, authenticity_token: form_authenticity_token, can_add_group: current_user.can_create_patient? }) %>
